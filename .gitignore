# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
env/
build/
dist/
*.egg-info/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# SQLite (colocated with KB, outside repo)
*.db
*.db-journal
*.db-wal
*.db-shm

# Migration runner backups (db/migrations.py creates <db>.bak-<timestamp>,
# which doesn't match *.db above since it doesn't end in .db)
*.bak-*

# ChromaDB (colocated with KB, outside repo)
chroma/

# Knowledge base (outside repo, user-managed)
meridian-kb/

# pytest
.pytest_cache/

# Coverage
htmlcov/
.coverage

# Local environment / secrets
.env
.env.*

# Logs
*.log

# Local MCP client config — contains this machine's absolute venv path,
# never portable across machines. Track opencode.json.example instead.
opencode.json
