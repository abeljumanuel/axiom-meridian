[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "axiom-meridian"
version = "0.1.0"
description = "Knowledge audit MCP server — Code drifts. Meridian doesn't."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Axiom JUMA" }]

dependencies = [
    "fastmcp>=0.4.0",
    "httpx>=0.27.0",
    "sentence-transformers>=3.0.0",
    "chromadb>=0.5.0",
    "torch>=2.2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "ruff>=0.5.0",
]

[project.scripts]
meridian = "meridian.__main__:main"
meridian-mcp = "meridian.server:run_stdio"

[tool.hatch.build.targets.wheel]
packages = ["src/meridian"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
markers = [
    "benchmark: performance regression guards (excluded from the default run; invoke explicitly with `-m benchmark`)",
]
addopts = "-m 'not benchmark'"

[tool.ruff]
target-version = "py311"
line-length = 100
